<span id="notifications_badge">
  <% if count.to_i > 0 %>
    <span class="inline-flex items-center justify-center min-w-[1.25rem] h-5 px-1.5 rounded-full bg-rose-500 text-white text-xs font-bold align-middle ml-1">
      <%= count %>
    </span>
  <% end %>
</span>
